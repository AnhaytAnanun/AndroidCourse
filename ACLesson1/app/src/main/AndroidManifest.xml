<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flycode.aclesson1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".TextViewExamplesActivity" />
        <activity android:name=".ButtonExamplesActivity" />
        <activity android:name=".StartExampleActivity" />
        <activity android:name=".ResultExampleActivity" />
        <activity android:name=".LinearLayoutExampleActivity" />
        <activity android:name=".RelativeLayoutExampleActivity"></activity>
    </application>

</manifest>